# ─────────────────────────────────────────────────────────────────
#  CreativeAI — Environment Variables
#  Copy this file to .env.local and fill in all values.
#  NEVER commit .env.local to version control.
#  All variables marked [CLIENT] are exposed to the browser.
#  All variables marked [SERVER] must NEVER be exposed to the client.
# ─────────────────────────────────────────────────────────────────

# ── Supabase ──────────────────────────────────────────────────────
# Source: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co       # [CLIENT]
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here                # [CLIENT]
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here            # [SERVER] ⚠ NEVER expose

# ── AI Generation APIs ────────────────────────────────────────────
# Source: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_your-token-here                          # [SERVER]

# Source: https://fal.ai/dashboard/keys
FAL_API_KEY=your-fal-api-key-here                              # [SERVER]

# ── Stripe ────────────────────────────────────────────────────────
# Source: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_your-key-here                        # [SERVER] ⚠ Use sk_test_ in dev
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your-key-here       # [CLIENT] Use pk_test_ in dev

# Source: Stripe Dashboard → Webhooks → your endpoint → Signing secret
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here           # [SERVER]

# Stripe Price IDs — create these in Stripe Dashboard → Products
STRIPE_PRICE_SPARK=price_spark_monthly_id                      # [SERVER] $5/mo Spark plan
STRIPE_PRICE_GLOW=price_glow_monthly_id                        # [SERVER] $12/mo Glow plan
STRIPE_PRICE_PRO=price_pro_monthly_id                          # [SERVER] $19/mo Pro plan

# ── Cloudflare R2 ─────────────────────────────────────────────────
# Source: Cloudflare Dashboard → R2 → Your Bucket → Manage API tokens
CLOUDFLARE_R2_ACCESS_KEY=your-r2-access-key                    # [SERVER]
CLOUDFLARE_R2_SECRET_KEY=your-r2-secret-key                    # [SERVER]
CLOUDFLARE_R2_BUCKET_NAME=creativeai-outputs                   # [SERVER]
CLOUDFLARE_R2_ACCOUNT_ID=your-cloudflare-account-id           # [SERVER]
CLOUDFLARE_CDN_BASE_URL=https://cdn.yourcreativeai.com         # [SERVER] Public CDN URL

# ── Resend (Email) ────────────────────────────────────────────────
# Source: https://resend.com/api-keys
RESEND_API_KEY=re_your-api-key-here                            # [SERVER]
RESEND_FROM_EMAIL=hello@yourcreativeai.com                     # [SERVER]

# ── PostHog (Analytics) ───────────────────────────────────────────
# Source: https://app.posthog.com/project/api_key
NEXT_PUBLIC_POSTHOG_KEY=phc_your-key-here                      # [CLIENT]
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com               # [CLIENT]

# ── Sentry (Error Tracking) ───────────────────────────────────────
# Source: https://sentry.io → Your Project → Settings → DSN
SENTRY_DSN=https://your-key@sentry.io/your-project-id         # [CLIENT+SERVER]
SENTRY_ORG=your-sentry-org                                     # [SERVER] for source maps
SENTRY_PROJECT=creativeai                                       # [SERVER] for source maps

# ── Application ───────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://yourcreativeai.com                 # [CLIENT] Production URL
NEXT_PUBLIC_APP_NAME=CreativeAI                                # [CLIENT]
